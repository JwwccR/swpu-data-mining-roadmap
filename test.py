import pandas as pd
import numpy as np

print("Hello! Environment is working.")
data = pd.DataFrame({"Tool": ["Pandas", "NumPy", "Matplotlib", "PyTorch"], "Status": ["Installed", "Installed", "Installed", "Pending"]})
print(data)
